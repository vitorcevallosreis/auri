import { Poppins } from "next/font/google"

export const text_ligth = Poppins({
  weight: "100",
  subsets: ["latin"],
})

export const text_regular = Poppins({
  weight: "400",
  subsets: ["latin"],
})

export const text_bold = Poppins({
  weight: "600",
  subsets: ["latin"],
})
